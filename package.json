{
  "name": "novamind-hub",
  "version": "1.0.0",
  "private": true,
  "description": "NovaMind competition website with a secure OpenAI-powered assistant.",
  "scripts": {
    "start": "node server.js",
    "migrate": "node scripts/migrate.js",
    "check": "node --check server.js && node --check js/bundle.js && node --check js/ui-refresh.js && NODE_ENV=test node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.50.0",
    "pg": "8.16.3"
  }
}
