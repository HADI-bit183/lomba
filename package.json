{
  "name": "novamind-hub",
  "version": "1.0.0",
  "private": true,
  "description": "NovaMind competition website with a secure OpenAI-powered assistant.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check js/bundle.js && node --check js/ui-refresh.js"
  },
  "engines": {
    "node": ">=18"
  }
}
